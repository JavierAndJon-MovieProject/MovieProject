"use strict";

const option = {
    method: 'GET',
    headers: {
        'Content-Type': 'application/json'
    },
    // body : JSON.stringify({
    //
    // })
};

const MOVIES_URL = "https://oasis-roasted-indigo.glitch.me/movies"

// fetch(MOVIES_URL, option)
//     .then(response => response.json())
//     .then(response.map())
// $.get(MOVIES_URL).done
